import {Fragment} from "react";
import {Outlet} from "react-router-dom";
import "@/styles/scanning.scss"

export default function Scanning() {
    return <Fragment>
        <Outlet/>
    </Fragment>
}